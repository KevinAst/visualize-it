import React from 'react';
import ReactDOM from 'react-dom';
import 'index.css'; // ?? obsolete once we have material-ui in place
import App from 'App';

ReactDOM.render(<App />, document.getElementById('root'));

import {createFeature}    from 'feature-u';
import route              from './route';

// feature: tabManager
//          a manager of tabs
export default createFeature({
  name: 'tabManager',
  route,
});

import ChangeManager  from './ChangeManager';

// promote the changeManager feature public API
export {
  // the ChangeManager class (a reactive custom store) that manages and monitors changes in an EPkg
  // ... create one of these for each EPkg
  // ... see: ChangeManager.js for full documentation
  ChangeManager,
};
